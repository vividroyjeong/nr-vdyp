import { EnumValue } from './enum-value'
export interface ProgressFrequency {
  /**
   * @type {number}
   * @memberof ProgressFrequency
   */
  intValue?: number

  /**
   * @type {EnumValue}
   * @memberof ProgressFrequency
   */
  enumValue?: EnumValue
}
